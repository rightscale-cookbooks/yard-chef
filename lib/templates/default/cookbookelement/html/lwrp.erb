<div> 
  <h1>RightScale</h1>
  <hr>
  <ul>
    <% @cookbooks.each do |cookbook| %>
      <% @cookbook = cookbook %>
      <li>
        <h2><a href="<%= cookbook.name.to_s %>.html"><%= cookbook.name.to_s %></a></h2>
        <%= erb(:action_table) %>
        <%= erb(:attribute_table) %>
      </li>  
    <% end %>
  </ul>
</div>
