<ul class="summary compact">
  <% @objects.each do |library_file| %>
    <% libraries = find_library(library_file) %>
    <% libraries.each do |lib| %>
      <li><span class="summary_signature">
        <a href="../<%= url_for(lib, nil, false) %>"><%= lib %></a>
      </li>
    <% end %>
  <% end %>
</ul>
