<% n = 1 %>
<% @items.each do |item| %>
  <li class="r<%= n %>">
    <span class="object_link">
      <a href="<%= url_for(item) %>" title="<%= item.file %>">
        <%= h(item.name) %>
      </a>
    </span>
  </li>
  <% n = n == 2 ? 1 : 2 %>
<% end %>
